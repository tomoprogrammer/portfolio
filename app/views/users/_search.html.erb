
    <div class="col-md-8 offset-md-1">
      <h2>ユーザー一覧</h2>
        <thead>
          <tr>
            <th>画像</th>
            <th>ユーザー名</th>
            <th colspan="3"></th>
          </tr>
       </thead>
      <tbody>
        <% users.each do |user| %>
          <tr>
            <td>
              <%= attachment_image_tag user, :profile_image, :fill,40,40, format: 'jpag' %>
            </td>
            <td>
              <%= user.name %>
            </td>
            <td>
              <%= link_to '詳細', user_path(user.id) %>
            </td>
          </tr>
        <% end %>
      </tbody>