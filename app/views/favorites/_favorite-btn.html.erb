<% if walk.favorited_by?(current_user) %>
  ♥<%= link_to walk.favorites.count, walk_favorites_path(walk), method: :delete, remote: true, class: "glyphicon glyphicon-heart" %>
<% else %>
  ♡<%= link_to walk.favorites.count, walk_favorites_path(walk), method: :post, remote: true, class: "glyphicon glyphicon-heart-empty" %>
<% end %>